<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hotel"
        tools:targetApi="31">

        <!-- Các activity phải được khai báo trực tiếp trong <application> -->
        <activity android:name=".DeleteOfList"
            tools:ignore="WrongManifestParent" />
        <activity android:name=".ViewInfo"
            tools:ignore="WrongManifestParent" />
        <activity android:name=".AddH"
            tools:ignore="WrongManifestParent" />
        <activity android:name=".DisplayH"
            tools:ignore="WrongManifestParent" />
        <activity android:name=".Signup"
            tools:ignore="WrongManifestParent" />

        <!-- MainActivity đặt cuối cùng để có intent-filter -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
